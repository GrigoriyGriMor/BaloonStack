using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MultipleTextPro : MonoBehaviour
{

    [HideInInspector]
    public Transform transformObject;

    // Start is called before the first frame update
    void Start()
    {

    }

    private void Update()
    {
       // if (transformObject.gameObject.activeInHierarchy)
       // {
           // GetComponent<RectTransform>().position = Camera.main.WorldToScreenPoint(transformObject.position);
       // }
       // else
       // {
       //     gameObject.SetActive(false);
      //  }

    }
}
